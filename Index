<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Working Links</title>
    <style>
        body {
            background-color: black; /* Set background color to black */
            color: white; /* Set text color to white */
            font-family: Arial, sans-serif; /* Set font style */
            margin: 0; /* Remove default margin */
            height: 100%; /* Ensure full height for body */
            overflow-x: hidden; /* Hide horizontal scroll */
        }

        /* Center the text in the middle of the page */
        .centered-text {
            font-size: 2rem; /* Set font size */
            font-weight: bold; /* Make the text bold */
            display: flex;
            justify-content: center; /* Center horizontally */
            align-items: center; /* Center vertically */
            height: 100vh; /* Full viewport height */
            text-align: center; /* Ensure text is centered */
        }

        /* Styling for the scrollable content */
        .scrollable-content {
            padding: 20px;
            text-align: center;
        }

        .scrollable-content h2 {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .scrollable-content p {
            font-size: 1rem;
            margin: 5px 0;
        }

        .scrollable-content a {
            color: #4CAF50; /* Green color for links */
            text-decoration: none; /* Remove underline */
        }

        /* Styling for the ad warning message */
        .ad-warning {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <!-- Centered initial text -->
    <div class="centered-text">
        <p>￫Working links￩</p>
    </div>

    <!-- Scrollable content below -->
    <div class="scrollable-content">
        <p>￬ ￬</p>
        <h2>Some ads are a bit weird</h2>
        <p>Avoid them or turn them Off in settings.</p>

        <div>
            <h2>Doge unblocker</h2>
            <p>Link 1 (if it sends you to another tab, close it, it's an AD)</p>
        </div>

        <div>
            <h2>Holy-Unblocker</h2>
            <p>Link 1</p>
            <p>Link 2</p>
        </div>

        <div>
            <h2>AnuraOS</h2>
            <p>Link 1</p>
        </div>

        <div>
            <h2>Net-shield</h2>
            <p>1. <a href="https://rb.gy/8lq4kp" target="_blank">https://rb.gy/8lq4kp</a></p>
        </div>

      
